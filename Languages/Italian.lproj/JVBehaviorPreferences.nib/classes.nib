{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                changeMessageScrollback = id; 
                changeRejoinDelay = id; 
                changeSendHistory = id; 
                changeSendOnEnterAction = id; 
                changeSendOnReturnAction = id; 
            }; 
            CLASS = JVBehaviorPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                autoRejoinCheck = NSButton; 
                enterKeyAction = NSPopUpButton; 
                messageScrollback = NSTextField; 
                messageScrollbackStepper = NSStepper; 
                rejoinDelay = NSTextField; 
                rejoinDelayStepper = NSStepper; 
                returnKeyAction = NSPopUpButton; 
                sendHistory = NSTextField; 
                sendHistoryStepper = NSStepper; 
            }; 
            SUPERCLASS = NSPreferencesModule; 
        }, 
        {
            ACTIONS = {apply = id; cancel = id; ok = id; selectModule = id; }; 
            CLASS = NSPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                applyButton = id; 
                cancelButton = id; 
                moduleMatrix = id; 
                okButton = id; 
                preferencesPanel = id; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = NSPreferencesModule; 
            LANGUAGE = ObjC; 
            OUTLETS = {"_preferencesView" = NSBox; preferencesView = NSBox; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}