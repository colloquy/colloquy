#include "target.xcconfig"

FRAMEWORK_SEARCH_PATHS = $(inherited) $(COLLOQUY_WORKSPACE_ROOT)/old-mac-binary-frameworks $(SYSTEM_LIBRARY_DIR)/PrivateFrameworks
HEADER_SEARCH_PATHS = $(SDKROOT)/usr/include/libxml2
